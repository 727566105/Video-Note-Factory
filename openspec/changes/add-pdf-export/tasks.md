# PDF 导出功能任务清单

## Phase 1: 基础功能

### 后端开发

- [x] **T1.1** 创建 `backend/app/routers/export.py` 路由文件
  - 定义 `GET /api/export/pdf/{task_id}` 端点
  - 实现文件存在性检查
  - 验证：路由注册成功，访问 404 返回正确错误

- [x] **T1.2** 实现 PDF 生成逻辑
  - 读取 Markdown 缓存文件
  - 使用 `markdown_pdf` 转换
  - 验证：简单 Markdown 能正确转换为 PDF

- [x] **T1.3** 添加 PDF 样式配置
  - 创建 CSS 样式常量
  - 配置中文字体支持
  - 验证：中文内容显示正常，样式美观

- [x] **T1.4** 注册路由到主应用
  - 在 `backend/app/__init__.py` 中注册 export router
  - 验证：API 端点可访问

- [x] **T1.5** 添加错误处理和日志
  - 处理文件不存在情况
  - 处理转换失败情况
  - 添加适当的日志记录
  - 验证：错误场景有清晰的错误消息

- [ ] **T1.6** 编写单元测试
  - 测试 PDF 生成函数
  - 测试错误处理
  - 验证：测试覆盖率 > 80%（待实施）

### 前端开发

- [x] **T1.7** 创建导出按钮组件
  - 创建 `ExportPDFButton.tsx`
  - 实现下载逻辑
  - 添加加载状态
  - 验证：组件可独立运行

- [x] **T1.8** 集成到笔记详情页
  - 在 `MarkdownHeader.tsx` 中添加导出按钮
  - 传递 taskId 参数
  - 验证：按钮在正确位置显示

- [x] **T1.9** 添加 API 请求封装
  - 使用原生 fetch API
  - 验证：API 调用成功

- [x] **T1.10** 添加用户反馈
  - 成功提示
  - 错误提示
  - 加载状态
  - 验证：所有状态都有合适的反馈

- [x] **T1.11** 响应式适配
  - 移动端按钮样式适配
  - 验证：在不同屏幕尺寸下显示正常

### 测试

- [ ] **T1.12** 集成测试
  - 测试完整导出流程
  - 测试边界情况（大文件、特殊字符等）
  - 验证：所有测试通过（待用户测试）

- [ ] **T1.13** 用户体验测试
  - 测试导出速度
  - 测试 PDF 质量
  - 验证：用户体验良好（待用户测试）

## Phase 2: 优化增强（可选）

- [ ] **T2.1** 添加 PDF 缓存机制
  - 缓存生成的 PDF 文件
  - 避免重复转换

- [ ] **T2.2** 支持自定义样式选项
  - 提供多种样式模板
  - 用户可选择样式

- [ ] **T2.3** 批量导出功能
  - 支持选择多个笔记批量导出
  - 打包为 ZIP 下载

- [ ] **T2.4** 导出历史记录
  - 记录导出操作
  - 提供重新下载链接

## 依赖关系

```
T1.1 → T1.2 → T1.3 → T1.4
                ↓
              T1.5
                ↓
              T1.6

T1.7 → T1.8 → T1.9 → T1.10 → T1.11
                           ↓
                         T1.12
                           ↓
                         T1.13
```

## 验收标准

### 功能验收
- [x] 用户可以点击按钮导出 PDF
- [ ] PDF 内容与 Markdown 内容一致（待用户测试）
- [ ] 中文显示正常（待用户测试）
- [ ] 样式美观可读（待用户测试）
- [x] 错误情况有友好提示

### 性能验收
- [ ] 普通笔记（<5000字）导出时间 < 5秒（待用户测试）
- [ ] 大笔记（>5000字）导出时间 < 15秒（待用户测试）

### 兼容性验收
- [ ] Chrome 浏览器正常工作（待用户测试）
- [ ] Safari 浏览器正常工作（待用户测试）
- [ ] 移动端浏览器正常工作（待用户测试）
